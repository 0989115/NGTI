
@model NGTI.Models.bhvViewModel;

@{
    ViewData["Title"] = "Index";
}

<div class="text-center">
    <table class="table" style="width: 50%; margin-left: 30%; margin-top:15%;">
        <tbody>
            @if(Model.bhver == false)
            {
            <tr>
                <th scope="row">Er is morgen geen BHV'er aanwezig. Zo mogelijk plan uw zelf nu in!</th>
            </tr>
            }
        </tbody>
    </table>
</div>
